This pattern act as a bridge between the two interfaces. Once class will have a is-a relation as well as a has-a relation
with the two interfaces to make a bridge between them.

Client(Main) will make a object of adapter and adapter will be connected to the adaptee.